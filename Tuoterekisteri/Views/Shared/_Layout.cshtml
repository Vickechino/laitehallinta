<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Careeria</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
               @Html.ActionLink("Careeria" , "Index", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="" role="button" aria-haspopup="true" aria-expanded="false">@(T.txt[0, L.nr])</a>
                        <div class="dropdown-menu" style="">
                            @for (int i = 0; i < L.tot; i++)
                            {
                                if (i != L.nr)
                                {
                                    <ul class="dropdown-item"> @Html.ActionLink((T.txt[0, i]), (T.txt[0, i]), "Home") </ul>
                                }
                            }

                        </div>
                    </li>

                    @if (Session["UserName"] == null)
                    {
                        <li>@Html.ActionLink(T.txt[1, L.nr], "Login", "Users")</li>
                    }
                    else
                    {
                       <li>@Html.ActionLink(T.txt[6, L.nr], "Logout", "Users")</li>
                    }

                    @if (Session["Permission"] != null && Session["Permission"].ToString() == "1")
                    {
                          <li class="nav-item dropdown">
                              <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="" role="button" aria-haspopup="true" aria-expanded="false">Admin</a>
                              <div class="dropdown-menu" style="">
                                  <ul class="dropdown-item">@Html.ActionLink(T.txt[12, L.nr], "Index", "Users")</ul>
                                  <ul class="dropdown-item">@Html.ActionLink(T.txt[13, L.nr], "Index", "Locations")</ul>
                                  <ul class="dropdown-item">@Html.ActionLink(T.txt[7, L.nr], "Index", "Products")</ul>
                                  <ul class="dropdown-item">@Html.ActionLink(T.txt[20, L.nr], "Index", "Productgroups")</ul>
                                  <ul class="dropdown-item">@Html.ActionLink(T.txt[29, L.nr], "Index", "Specifications")</ul>
                              </div>
                          </li>
                     }
                        
                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>

        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
